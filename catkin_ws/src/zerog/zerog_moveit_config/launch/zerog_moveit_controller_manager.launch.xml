<launch>

  <!-- MRM: 08/01/2022: Adding the "gazebo_simulation" arg so that there is not need to create more than one moveit configs and so that appropriate ros_controllers are loaded when launching moveit
  without messing with the ros_controllers for real hardware" -->
  <arg name="gazebo_simulation" default="false"/>

  <!-- loads moveit_controller_manager on the parameter server which is taken as argument
    if no argument is passed, moveit_simple_controller_manager will be set -->
  <arg name="moveit_controller_manager" default="moveit_simple_controller_manager/MoveItSimpleControllerManager" />
  <param name="moveit_controller_manager" value="$(arg moveit_controller_manager)"/>
   <!--<param name="use_controller_manager" value="false"/>-->
  <!--<param name="trajectory_execution/execution_duration_monitoring" value="false"/> -->
  <!-- loads ros_controllers to the param server -->
  <!-- MRM: 08/01/2022: using conditionals for either real hardware or gazebo -->
  <group if="$(arg gazebo_simulation)">
    <rosparam file="$(find zerog_moveit_config)/config/ros_gazebo_controllers.yaml"/> 
  </group>
  <group unless="$(arg gazebo_simulation)">
    <rosparam file="$(find zerog_moveit_config)/config/ros_controllers.yaml"/> 
  </group>

  

</launch>
